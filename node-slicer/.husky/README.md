# Git Hooks

This directory contains Git hooks managed by Husky.

## Setup

Run `pnpm install` to set up the hooks automatically.

## Available Hooks

- **pre-commit**: Runs lint-staged to format and lint staged files before committing
