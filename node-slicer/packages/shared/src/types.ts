/**
 * Shared TypeScript types for Node-Slicer
 *
 * These types are used across both frontend and backend to ensure
 * type safety in the communication between them.
 */

// Placeholder for shared types
// Will be populated in later phases
export type NodeType = string;
export type EdgeType = string;
